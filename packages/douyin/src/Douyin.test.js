const assert = require('assert')
const Douyin = require('.')

/* eslint-env jest */
describe('Douyin', () => {
  describe('constructor', () => {
    it('shouldSuccess', () => {
      const douyin = new Douyin()
    })
  })
})
